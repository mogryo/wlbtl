{
	"name": "wlbtl",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"dev": "vite",
		"build": "tsc && vite build",
		"preview": "vite preview",
		"biome:format": "yarn biome format --write",
		"biome:safe": "yarn biome lint --write",
		"biome:unsafe": "yarn biome lint --unsafe --write",
		"biome:safe:format": "yarn biome check --write",
		"biome:unsafe:format": "yarn biome check --unsafe --write"
	},
	"devDependencies": {
		"@biomejs/biome": "1.9.4",
		"typescript": "~5.6.2",
		"vite": "^6.0.1",
		"vite-tsconfig-paths": "^5.1.4"
	},
	"dependencies": {
		"phaser": "^3.87.0"
	}
}
